export default function ChatbotLogsPage() {
  return (
    <section className="space-y-4">
      <header>
        <h2 className="text-base font-semibold text-slate-900">Chatbot (log)</h2>
        <p className="text-sm text-slate-500">Logs du chatbot (endpoint introuvable dans ton backend).</p>
      </header>

      <div className="card p-6 text-sm text-slate-700">
        Aucun endpoint “chatbot / logs” n’a été trouvé dans les routes backend du projet.
      </div>
    </section>
  );
}

